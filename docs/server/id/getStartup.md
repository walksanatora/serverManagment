# The getStartup endpoint

full path: /server/<id\>/getStartup
## request types and responses

headers:

status:     
`200` - request finished successfully <br>

upon a `*` request to this endpoint, startup command (one liner)