# The lsPorts endpoint

full path: /server/<id\>/lsPorts
## request types and responses

headers:

status:     
`200` - request finished successfully <br>

upon a `*` request to this endpoint, they key-value pairs of ports (`container: host`)